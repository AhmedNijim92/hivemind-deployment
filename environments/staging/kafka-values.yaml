# Bitnami Kafka chart overrides for dev
replicaCount: 1

persistence:
  enabled: true
  size: 8Gi

zookeeper:
  replicaCount: 1

resources:
  requests:
    memory: "512Mi"
    cpu: "250m"
  limits:
    memory: "1Gi"
    cpu: "500m"

