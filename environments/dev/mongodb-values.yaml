# Bitnami MongoDB chart overrides for dev
architecture: standalone

auth:
  enabled: false

persistence:
  enabled: true
  size: 4Gi

resources:
  requests:
    memory: "256Mi"
    cpu: "100m"
  limits:
    memory: "512Mi"
    cpu: "250m"
