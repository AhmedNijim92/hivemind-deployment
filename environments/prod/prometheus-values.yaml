# Prometheus chart overrides for dev
server:
  persistentVolume:
    enabled: true
    size: 8Gi
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "250m"

alertmanager:
  enabled: false

pushgateway:
  enabled: false

